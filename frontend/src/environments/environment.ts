/**
 * Development environment configuration.
 *
 * API_URL points to the local Rust backend during development.
 */

export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
};
