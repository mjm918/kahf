# KahfLane local development reverse proxy.
# Proxies /api to the backend and everything else to the Angular dev server.

:80 {
	handle /api/* {
		reverse_proxy host.docker.internal:3000
	}

	handle {
		reverse_proxy host.docker.internal:4200
	}
}
